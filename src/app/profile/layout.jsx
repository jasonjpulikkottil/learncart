export const metadata = {
    title: 'Profile | LearnCart',
    description: 'Manage your LearnCart profile settings',
};

export default function ProfileLayout({ children }) {
    return children;
}
