export const metadata = {
    title: 'Create Account | LearnCart',
    description: 'Create a LearnCart account to start buying, selling, and swapping items on campus.',
};

export default function RegisterLayout({ children }) {
    return children;
}
