export const metadata = {
    title: 'Edit Listing | LearnCart',
    description: 'Update your listing details on LearnCart.',
};

export default function EditListingLayout({ children }) {
    return children;
}
